# CPU dev: pip install --index-url https://download.pytorch.org/whl/cpu torch
# GPU: install the CUDA build of torch that matches the driver, then the rest.
torch>=2.4
transformers>=4.45
datasets>=3.0
accelerate>=0.34
numpy>=1.26
scipy>=1.11
pandas>=2.0
matplotlib>=3.8
tqdm
python-dotenv
pyyaml
huggingface_hub>=0.25
pytest>=8.0
